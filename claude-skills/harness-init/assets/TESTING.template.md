# Testing

## 命令

```bash
./scripts/check.sh   # typecheck + lint
./scripts/test.sh    # 全部测试
./scripts/smoke.sh   # 冒烟
```

<列出真实存在的命令；如需跑单个测试，写明用法>

## 策略

- 单元测试覆盖纯逻辑。
- 集成测试覆盖模块边界。
- smoke / e2e 覆盖关键用户路径。

## 不允许回归的关键路径

1. <路径 A：从用户视角描述>
2. <路径 B>

## 何时必须加/改测试

- 新增用户可见行为。
- 修 bug（先写复现测试）。
- 改 API 契约或数据模型。
- 改鉴权、支付、权限等敏感代码。

## 手工验证

UI 改动尽量在真实浏览器里走一遍受影响路径（能用 Playwright/DevTools 自动化就自动化），并在进度记录中写明验证了哪条路径、什么环境。
